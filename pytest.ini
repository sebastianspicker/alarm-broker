[pytest]
testpaths =
  services/escalane/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
  unit: Fast isolated tests without external integrations.
filterwarnings =
  ignore:ADMIN_API_KEY is not set.*:UserWarning
